# Workspace Rules for Bridge

- **Git Update Checking**: At the start of every new session or task, always run `git pull` (or check for updates) to stay in sync with the remote repository.
- **Git Push on Completion**: When a task is complete or at the end of a session, ensure all code changes are committed and pushed to the remote GitHub repository.
